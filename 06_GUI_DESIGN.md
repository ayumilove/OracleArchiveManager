# GUI 设计

本文以主界面设计 demo（版本 1.0.0，见 `docs/ui_main_window_demo.png`）为实现基线。

## 1. 总体布局

```text
┌──────────────────────────────────────────────────────────────┐
│ 标题栏：Logo + Oracle Archive Manager <版本>      设置 帮助 关于 │
├───────────┬──────────────────────────────────────────────────┤
│           │                                                  │
│  深色     │              内容区（白色 Card）                   │
│  左侧     │                                                  │
│  导航     │                                                  │
│           │                                                  │
├───────────┴──────────────────────────────────────────────────┤
│ 状态栏：● 就绪 ｜ 当前用户：xxx ｜ archive_manager.db           │
└──────────────────────────────────────────────────────────────┘
```

- 窗口建议 1280 x 800，支持高 DPI；
- 设置 / 帮助 / 关于置于顶栏右侧，不占左侧导航；
- 状态栏左侧显示就绪状态（就绪 / 异常），右侧显示当前用户与控制库文件名。

## 2. 左侧导航

- 首页（仪表盘）
- 任务管理
- 运行管理
- 连接管理
- 日志查看
- 系统设置

## 3. 首页（仪表盘）

首页为复合界面：左侧连接概览与任务列表，右侧任务详情与当前运行。

### 3.1 数据库连接概览 Card

- 连接名称 + 角色（生产库 / 归档库）；
- host:port/服务名、Schema；
- 状态 Badge：已连接（绿）/ 未连接（红）；
- 新建按钮。

### 3.2 任务列表 Card

- 搜索框：按任务名称或表名过滤；
- 任务条目：任务名、源表 → 目标表、启用/禁用 Badge；
- 新建任务、刷新按钮。

### 3.3 任务详情

顶部动作按钮：编辑 / 复制（复制配置为新任务）/ 删除 / 运行任务（主色）。

Tabs：

| Tab | 说明 |
|---|---|
| 任务配置 | 源表、目标表、任务统计（预估）三张 Card |
| 运行历史 | 该任务的历史 Run 列表（取代独立历史页） |
| 调度设置 | V1 预留占位（显示“即将推出”），调度能力见 P2 |
| 高级设置 | 其余高级参数 |

源表（生产库）Card：连接、Schema、表名、归档日期字段（可选；留空 = 按附加 WHERE 或全表归档，见 04 §6）、保留月份、归档条件（表达式模板；实际 cutoff 在 Run 创建时冻结，见 04 §6）、主键列。

目标表（归档库）Card：连接、Schema、表名、目标表操作（如果不存在则创建，默认关闭，见 05 §11）、保持表结构一致（强制校验，见 05 §7）、批量大小、校验方式（COUNT / PK / HASH）。

任务统计（预估）Card：待归档行数、数据大小（预估）、索引大小（预估）、预计批次数、预计耗时；重新分析按钮（触发 Analyze / Dry Run，见 04 §1）。

### 3.4 当前运行面板

- 标题：Run ID + 状态 Badge（如“正在校验 (Batch 318/657)”）；
- 动作按钮：暂停 / 停止（Safe Stop）；
- 五步进度条：

```text
分析 → 复制数据 → 校验数据 → 清理预览 → 清理生产库
```

每步显示 完成 / 进行中 / 等待中 及时间戳，复制 / 校验步附行数进度；

- 总进度条与实时统计：总行数、已复制、已校验、已删除、失败行数（红）、当前批次、批次大小、耗时、预计剩余；
- 运行日志面板：列 时间 / 级别 / 阶段 / 消息；支持 清空日志、自动滚动；
- 批次列表面板：列 批次号 / 状态 / 选择行数 / 复制行数 / 校验行数 / 删除行数 / 开始时间 / 耗时；状态 Badge 已完成 / 进行中 / 等待中 / 失败；刷新按钮。

无活动 Run 时，面板显示最近一次 Run 摘要与 Resume 入口。

## 4. 连接管理页

字段：

- 名称
- Host
- Port
- Service Name
- Username
- Password / Credential
- Role（SOURCE / TARGET / BOTH）

按钮：

- 测试
- 保存
- 编辑
- 删除

## 5. 运行管理页

- Run 列表：Run ID、任务、状态、开始时间、进度；
- 点击 Run 跳转首页当前运行面板视图，复用同一面板组件。

## 6. 日志查看页

- 按 Run / 级别 / 阶段 过滤；
- 导出日志。

## 7. Purge Preview（清理预览）

必须单独窗口，由“清理预览”步骤或 Purge 按钮进入：

显示：

- Source database；
- Schema/Table；
- Cutoff；
- Verified rows；
- Expected DELETE rows；
- Run ID。

用户必须输入表名确认。

## 8. UI 风格

- Light Theme，Windows 现代桌面风格；
- 深色左侧导航，内容区白色 Card；
- 主色蓝：主按钮、进行中状态；
- 成功绿：已完成 / 已连接 / 启用；
- 危险红：停止、失败行数、Purge 警示（不滥用颜色）；
- 中性灰：等待中 / 禁用；
- 状态统一使用 Badge；
- 数字千分位显示，耗时 hh:mm:ss。
